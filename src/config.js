//const domain = (window.location.hostname === 'localhost' ? 'localhost:8000' : 'api.minoto.com');

const domain = 'api.minoto.com';

export const apiBase = `https://${domain}/v1/shared/`
export const storagePath = 'https://minoto-test.ams3.digitaloceanspaces.com/'
export const basePath = 'https://minoto.com'